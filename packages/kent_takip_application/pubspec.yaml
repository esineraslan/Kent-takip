name: kent_takip_application
description: Shared command, projection and sync contracts for Kent Takip demos.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.13.0 <4.0.0"

dependencies:
  kent_takip_contracts:
    path: ../kent_takip_contracts
  kent_takip_domain:
    path: ../kent_takip_domain
  kent_takip_persistence:
    path: ../kent_takip_persistence

dev_dependencies:
  test: ^1.26.3
